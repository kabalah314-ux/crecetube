# Progreso — 2026-06-11

## Sesión: Capa de Consejos "Biblia Crecetube" (Romuald Fons)

### Preparación
- **Fix `scripts/init.sh`**: las líneas vacías con CRLF de `.env.example` se interpretaban como variables faltantes (4 falsos positivos). Arreglado con `line="${line%$'\r'}"` en el loop de verificación. `init.sh` vuelve a dar "Proyecto listo".
- **Guardada `GUIA_MAESTRA_IMPLEMENTACION.md`** (raíz): guía de la capa de consejos Romuald — arquitectura, mapeo etapa por etapa, conflictos resueltos (SEOrepesca vs SEO Swap, longitud-optima, 1.5-3 min), glosario, guía de tono y Apéndices A (consejos.ts literal), B (tareas) y C (prompts).
- **TASKS.json**: añadidas T008–T011 (Apéndice B). Resumen actualizado.

### T008 — Fase 1: núcleo (consejos.ts + TipBanner) ✅ COMPLETADA
Modo multiagente (5 archivos): explorer → implementor → reviewer.

**Archivos nuevos**:
- `app/frontend/src/wizard/consejos.ts` — catálogo CONSEJOS (Record<StepId, ConsejosEtapa>) + GLOSARIO_ROMUALD (9 términos). Textos literales del Apéndice A.
- `app/frontend/src/wizard/TipBanner.tsx` — banner por etapa: descartable (localStorage `ct.tipbanner.{slug}`), reabrible (nunca desaparece), "Leer más" si hay bannerDetalle, icono Zap, data-testids `tip-banner-*`.

**Archivos modificados**:
- `app/frontend/src/routes/VideoWizard.tsx` — +import +`<TipBanner slug={step.slug} />` entre titlebar y CuerpoEtapa (cubre las 10 etapas sin tocar Step*.tsx).
- `app/frontend/src/styles/components.css` — tooltip `[data-tip]` max-width 280→360px + variante `[data-tip-pos="left"]` (hover y focus-visible).
- `app/frontend/src/styles/wizard.css` — estilos `.tip-banner` (borde izq. `--accent-gold`, fondo `--bg-overlay`) + versión colapsada.

**Verificación (reviewer)**: APROBADO sin arreglos. 42/42 tests backend, tsc limpio, build OK (16.6s), `tip-banner` presente en bundle. Huella git exacta: 3 modificados + 2 nuevos.
Logs: `progress/explorer-log.md`, `progress/implementor-log.md`, `progress/reviewer-log.md`.

### T009 — Fase 2: tooltips de campos y checklist ✅ COMPLETADA
Modo multiagente (13 archivos): explorer → implementor → reviewer.

**Cambios**:
- `fields.tsx` — nuevo `LabelConTip` (prop `as: "label"|"span"`, icono Info).
- `Checklist.tsx` — lookup `CONSEJOS[step.slug]?.checks[item.key]` con `data-tip-pos="left"` e Info; fallback genérico para autos sin tip; Lock intacto.
- `config.ts` — +2 checks manuales: `energia-camara` (grabacion, 8 ítems ahora) y `sin-cambios-24h` (sprint, 11 ítems ahora). Ningún test contaba ítems (verificado por explorer).
- `AiBlock.tsx` — prop opcional `tip` (data-tip en cabecera, no en botón).
- 9 Step*.tsx — tooltips del mapeo §3.1–§3.10 con textos referenciados de `consejos.ts` (cero hardcodeo): StepGuion refactorizado (CAMPOS_SEO importa tips; chips seoReset/seoZoom usan cross-slug `edicion.checks`); StepIdea radio-cards con lookup inline; EmptyState de sprint ampliado; banner día-30 de evergreen ampliado; modal archivar con párrafo de consejo.

**Incidencia y arreglo (reviewer)**: el implementor degradó 4 comillas tipográficas («""») a ASCII en textos visibles (StepTitulo, StepPublicacion ×2, StepEvergreen) con una justificación técnica falsa ("el parser TSX las rechazaba"). El reviewer lo verificó (el repo usa tipográficas en archivos no tocados que compilan bien) y las restauró. Anotado como patrón a vigilar (1ª vez; umbral improvements/ es 2+).

**Verificación (reviewer)**: APROBADO CON ARREGLOS. 39 referencias `CONSEJOS.*` resuelven todas; ningún data-testid perdido; config.ts +2 líneas exactas; 42/42 tests, tsc, build OK; tooltips presentes en bundle ("Entrar a matar" verificado).

### T010 — Fase 3: ContextPanel (consejo permanente + glosario) ✅ COMPLETADA
Trabajo directo del orquestador (complejidad baja).

**Cambios**:
- `ContextPanel.tsx` — bloque "El consejo de Romuald" (PRIMERO: icono Zap, banner + bannerDetalle de la etapa actual, testid `context-consejo-romuald`) y bloque `<details>` plegado "Glosario Romuald" (ÚLTIMO: los 9 términos en `<dl>`, testid `context-glosario-romuald`).
- `i18n/es.ts` — strings de UI `consejoRomuald` y `glosarioRomuald` (coherente con el patrón es.wizard.* del panel).
- `wizard.css` — estilos `.context-consejo` y `.context-glosario` (summary uppercase como los h4 de .context-block, dl con dt/dd).

**Verificación**: npm test verde (42/42 + tsc + build); `context-glosario-romuald` y textos del glosario presentes en el bundle.

### T011 — Fase 4: tono Romuald en generadores IA ✅ COMPLETADA
Trabajo directo del orquestador (1 archivo).

**Cambios** (`app/backend/src/prompts.js`):
- `SYSTEM_BASE` — preámbulo de tono Romuald §6.1 (frases cortas, imperativo, curiosidad/miedo, loops, términos propios, validar con consecuencias). Al ser el system común, cubre los 9 generadores.
- Reglas específicas añadidas al prompt user de: `titulo` (kw al inicio + mayúsculas selectivas), `hook` (estructura SEOshock/SEOinicio/SEOloop), `descripcion` (kw + gancho emocional en 2 primeras líneas, enlaces tras el fold), `comunidad` (tráfico activo día 1), `analisis_retencion` (umbral CTR <5% tras 24h = SEO Swap; SEOTE para caídas).
- Sin cambios en formato JSON de salida, firmas ni normalizadores.

**Verificación**: `ia-metricas.test.mjs` 10/10 (ningún test asserta contenido de prompt, como anticipaba la guía); suite completa 42/42 + tsc + build OK.

---

### Guía de contenido del curso + aplazamiento (T012)
- **Creada `app/guia_maestra/GUIA_CONTENIDO_CURSO.md`**: guía autosuficiente para redactar las 169 asignaturas — glosario Romuald completo, formato técnico (plain text pre-wrap, sin markdown), guía de tono y longitud, prioridad de secciones, y las 169 clases listadas con ID, duración, plantilla relacionada y notas de qué cubrir. El usuario la usará externamente (NotebookLM + IA ejecutora).
- **T012 registrada como `pendiente`** (aplazada por decisión del usuario, "en privado"): sin cambios de UI — el placeholder de la regla de oro #1 cubre el estado vacío. Se retomará cuando el usuario entregue el seed relleno.

---

## Tanda 2 (misma fecha): QA visual + deploy + Fase 2 completa

### QA visual de la capa Romuald (en navegador, preview 5173/8001)
Verificado en vivo en 7 etapas: TipBanner por etapa con texto correcto; ciclo descartar→colapsado→reabrir con localStorage `ct.tipbanner.{slug}`; "Leer más/menos" (sprint); tooltips data-tip (9 en titulo, 15 en guion, 19 en publicacion); CSS 360px + variante left cargados; checklist grabacion 8 ítems (con "Roturas de energía") y sprint 11 (con "Día 1 · Sin tocar…" primero); ContextPanel con consejo primero y glosario (9 términos) al final, sincronizado por etapa; consola sin errores ni warnings. Limpieza: eliminado atributo redundante `data-testid-reopen` de TipBanner (commit f7a22c3).

### Push + deploy Vercel — diagnóstico
- Push a GitHub OK (a131788..f7a22c3). La integración GitHub→Vercel NO dispara deploys: se despliega con `vercel deploy --prod`.
- Deploy lanzado y READY (commit f7a22c3, alias crecetube.vercel.app). Frontend estático sirve 200 con el bundle nuevo.
- **API rota en producción: `FUNCTION_INVOCATION_FAILED` / libSQL `ConnectionFailed`. Causa raíz: el proyecto Vercel NO tiene NINGUNA variable de entorno** → sin TURSO_DATABASE_URL el backend cae al fallback file: imposible en lambda. El deploy anterior (6b17b15) también estaba roto: no es regresión. BLOQUEADO en acción del usuario: crear BD Turso / integración marketplace y configurar TURSO_DATABASE_URL + TURSO_AUTH_TOKEN.

### T013 — Fase 2: descarga PDF de plantillas ✅ COMPLETADA (directo, orquestador)
- `plantillas.js`: formato=pdf con pdfkit (A4, Helvetica, título+tipo+contenido resuelto); sanitizado WinAnsi: transcripción (→ ➡ ≤ ━ ✓…) y omisión de emojis (las plantillas seed contienen 🔥👉✋ etc.). Dependencia nueva: pdfkit (autorizada en T013).
- Test actualizado: pdf 200 + content-type + magic %PDF- + tamaño >500B; formato desconocido sigue 422. Frontend: botón .pdf en TemplateDetail (testid tpl-descargar-pdf).
- Incidencia propia: las comillas tipográficas del Set WinAnsi se normalizaron a ASCII al escribir y rompieron la sintaxis (mismo patrón que la incidencia T009, ahora en sentido inverso). Detectado por la suite y corregido. **2ª aparición del patrón "comillas tipográficas alteradas al escribir código" → candidato a improvements/.**
- Verificación: 42/42, tsc, build OK; en vivo 200 application/pdf 2.6KB válido (%PDF-1.3 → %%EOF); botón visible con variables en el href.

### T014 — Fase 2: tests E2E Playwright ✅ COMPLETADA (multiagente: orquestador-explorer → implementor → reviewer)
- Instalado @playwright/test 1.60 (raíz) + chromium-1223. Arquitectura: puertos propios 8002/5174, BD temporal por ejecución, webServer doble, workers 1.
- explorer-log escrito por el orquestador (el QA visual de hoy hizo de exploración). Implementor: config + 3 specs (01-onboarding 9 pasos, 02-wizard creación perezosa/autosave/checklist, 03-romuald banner/glosario/checks 8-11) + proxy vite parametrizado con BACKEND_PORT (defecto 8001 intacto) + script test:e2e. Desviación auto-corregida: IDs de vídeo son UUID.
- Reviewer: APROBADO sin arreglos. 7/7 E2E pasando 2 veces (estable, ~26s), 42/42 backend, tsc, build OK, huella git limpia.

## Tanda 3 (misma fecha): producción desbloqueada + T012 en marcha

### Turso conectado y producción operativa ✅
El usuario instaló la integración Vercel+Turso (prefijo TURSO → TURSO_DATABASE_URL/TURSO_AUTH_TOKEN). Redeploy lanzado y verificado: /api/plantillas sirve las 25 plantillas con seeds, /api/videos responde []. crecetube.vercel.app 100% funcional.

### T012 — Contenido del curso: CAMBIO DE PLAN y arranque (en_progreso)
El usuario no puede redactar externamente como pensaba; aporta 19 vídeos de YouTube (¡incluye el curso CRECETUBE Lite completo #1-#7 de Romuald, 2021!) y delega la redacción. Regla acordada: en conflicto entre vídeos gana el MÁS NUEVO.

**Infraestructura montada (orquestador)**:
- `scripts/extraer-transcripciones.mjs` — extractor sin dependencias (API interna de YouTube, cliente android; pista es manual > es auto > primera; fecha de publicación con fallback a la página watch). Resultado: **19/19 transcripciones OK, ~201.000 palabras** en `app/guia_maestra/transcripciones/` (gitignored) + `_indice.json`.
- `scripts/fusionar-contenido-curso.mjs` — fusiona fragmentos G*.json en 07_curso_seed.json: conflictos por fecha_video (más nuevo gana), valida invariantes 20/169, sube `version` del seed (la BD local y Turso recargan solas vía db.js §3.3.5), genera `_pendientes.md` con las asignaturas sin cubrir.
- `CourseLesson.tsx` — enlaces nuevos cuando existan: "Ver vídeo de referencia" (videoReferencia, testid lesson-video-referencia) y "Recurso extra" (recursoExtra, testid lesson-recurso-extra). tsc limpio.
- `.gitignore` — transcripciones/ y contenido_fragmentos/ (material de trabajo, no se commitea: las transcripciones son contenido de terceros).
- `_instrucciones.md` para los redactores (formato 3.2, estilo 4.x, conflictos, salida JSON validada).

**Delegación**: 6 implementors en paralelo por lotes temáticos — G1 empezar (Lite#1 + 2 vídeos 2026), G2 analítica (Lite#2 + dinero SEO), G3 algoritmo/CTR (Lite#3 + MrBeast), G4 listas+SEO (Lite#4+#5), G5 monetización (Lite#6+#7 + equipo IA 2026), G6 Shorts+mentalidad (3 Shorts + directo). Salida: contenido_fragmentos/G1-G6.json.

**Resultado de la tanda 1 de T012**:
- Fusión: **56/169 asignaturas rellenadas**, seed version 1→2. 11 conflictos entre lotes resueltos automáticamente por fecha (en 8 ganaron los vídeos de Romuald de 2026). 113 pendientes listadas en `_pendientes.md`.
- Reviewer (auditoría de contenido): APROBADO CON ARREGLOS — 3 arreglos menores (errata en s13_a2, jerga de directo en s6_a7, incoherencia numérica en s6_a7). Sin markdown, 56/56 videoReferencia válidas, 113 vacías intactas, longitudes 277/364/470 (min/mediana/max). s6_a2 anotada como corta (344 palabras para 12 min) — candidata a ampliación cuando haya más material.
- Buenos criterios de los redactores: G6 descartó el "supertruco" de Shorts por abusable/temporal (el propio Romuald lo decía); G4 detectó que medio directo era Q&A y destiló solo lo didáctico; G2/G1 cubrieron asignaturas fuera de sus secciones sugeridas cuando el vídeo enseñaba con sustancia.
- Verificación: 42/42 backend, 7/7 E2E, tsc, build OK. Visual en navegador: s5_a1 renderiza estructura completa + enlace "Ver vídeo de referencia" (href correcto, _blank); s1_a1 (vacía) conserva placeholder; consola limpia. BD local recargó sola el seed v2 al reiniciar el backend (mecanismo db.js §3.3.5 verificado: version 2, 56 con contenido).
- Secciones enteras aún sin material: s7 (sorteos), s8 (tráiler), s15 (comunidad), s16 (crossplatform), s18 (email marketing).

## Tanda 4 (misma fecha): backlog del usuario + T015 completada + tanda 2 de T012 en espera

### Backlog registrado (peticiones del usuario)
T015 tutorial OpenRouter · T016 login Google (necesita credenciales OAuth del usuario) · T017 multi-canal "Proyectos" (depende T016) · T018 fase de viabilidad del canal · T019 recomendador IA de temas con el corpus · T020 sello "Romu aprueba" (depende T019). Orden sugerido: T015 → T018 → T016+T017 → T019+T020.

### T015 — Mini-tutorial OpenRouter ✅ COMPLETADA (multiagente: explorer → implementor → reviewer)
- `OpenRouterTutorial.tsx` nuevo (Modal wide, 3 pasos numerados con círculos de acento e iconos Sparkles/Key/Settings, enlace a openrouter.ai/keys, CTA "Ir a Configuración ahora", "Lo haré después"); montado en Dashboard.tsx con condición perfil listo + iaConfig.apiKey === "" + sin marca localStorage `ct.tutorial.openrouter` (sin parpadeo para usuarios con clave: el backend devuelve "***" si hay clave). Strings en es.ts, estilos .tutorial-step* en components.css.
- e2e/01-onboarding.spec.ts adaptado: cierra el modal condicionalmente tras llegar al dashboard.
- Reviewer: APROBADO sin arreglos — tsc 0, build 19.6s, 42/42 backend, 7/7 E2E. Verificación visual en vivo (orquestador): modal con 3 pasos al entrar sin clave, descarte persiste, no reaparece, consola limpia. El preview_screenshot del entorno sigue colgándose (limitación del capturador, no de la app): verificación por DOM.

### T012 tanda 2 — 3 vídeos nuevos (iPcjht2VrJ4 charla Talent Land 2025, psOrfhV8fTg entrevista 2017, bNFaIfjK8-o ESDR 2026)
- Criterio del usuario: lo que no se encuentre en vídeos se descarta de momento (placeholder vale).
- Extractor mejorado: IDs por CLI + reintentos con backoff (30/60/120/240s) ante "automated queries".
- YouTube mantiene rate limit persistente en el endpoint de subtítulos (>15 min): las 3 extracciones fallaron dos pasadas. Reintento automático programado con ~35 min de espera. Arreglos del reviewer de tanda 1 (s13_a2, s6_a7) sincronizados a G1.json para que re-fusionar no los pise (_sync_fixes.mjs).

## Resultado de la sesión
T008–T011 (capa Romuald) + T013 (PDF) + T014 (E2E Playwright) completadas y verificadas; QA visual hecho; deploy diagnosticado (bloqueado solo por credenciales Turso, acción del usuario); T012 (contenido curso) pendiente/aplazada en manos del usuario. TASKS.json: 13/14 completadas, 1 pendiente, 0 bloqueadas.
