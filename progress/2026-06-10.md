# Progreso — 2026-06-10

Proyecto: CRECETUBE Assistant
Estado general: COMPLETADO (T001–T007) · pendientes de fase 2: contenido didáctico de asignaturas, E2E Playwright, PDF de plantillas

---

## Tareas completadas

### [QA visual] Pasada por la app real en navegador (checklist 08 §8.9)
Qué se hizo: arranque con preview (backend :8001 + frontend :5173, `.claude/launch.json` nuevo) y recorrido como usuario real en viewport móvil (~665px):
- Onboarding completo de 9 pasos → perfil creado → dashboard con saludo y KPIs. ✓
- Creación perezosa: Enter en /videos/nuevo → POST → /wizard/idea con el título ya puesto (44/200). ✓
- Autosave 800ms verificado CONTRA LA API (brief persistido); items auto marcados y bloqueados, manual persistido. ✓
- Avance de estado automático al entrar en Título: idea → guion. ✓
- Curso: 20 secciones desde BD, borde con color de familia EXACTO (#BFA47A = --family-pasos), clase con placeholder regla de oro #1 y enlace a plantilla relacionada. ✓
- Plantillas: 25 en BD, preview en vivo resuelve variables y deja visibles las vacías `{nombre}`. ✓
- Tema claro/oscuro: cambia al vuelo y persiste en el perfil (PATCH verificado). ✓
- IA sin clave: botón Generar disabled + tooltip + enlace a Ajustes (regla de oro #5). ✓
- Consola: 0 errores (solo avisos informativos de React Router).

Bugs encontrados y ARREGLADOS en la pasada:
- Stepper en móvil quedaba oculto bajo el topbar al hacer scroll (sticky top:0 con z-index menor) → `position: static` en <768px (`wizard.css`).
- Avisos de future flags de React Router → activados `v7_startTransition` y `v7_relativeSplatPath` en `main.tsx`.
Re-verificado tras los fixes: tsc + build verdes.

Nota: la BD real (`app/backend/data/crecetube.db`) queda con datos de prueba del QA (perfil "Canal de Oscar" + 1 vídeo). Para estrenar la app desde cero: borrar ese archivo con la app parada.

### [T002–T007] Construcción completa de la app (6 sprints en una sesión)
Qué se hizo: app entera según la guía maestra con el stack fijado (React 18+Vite+TS / Express / better-sqlite3 documental, workspaces npm).
- **T002 Fundación**: monorepo, db.js con seeds versionados desde `app/guia_maestra/*.json`, API perfil con apiKey enmascarada, tokens CSS de 06 literales, layout sidebar/drawer, tema dark/light persistido, onboarding 9 pasos con borrador localStorage, 14 rutas.
- **T003 Wizard core**: API vídeos (merge parcial profundo, transiciones validadas, checklist, duplicar, soft delete+restaurar, upload miniatura), wizard data-driven (`wizard/config.ts` = checklists del 02 como código), autosave 800ms con reintentos y Ctrl+S, etapas 1–5 (chips, drag&drop guion, simulador grilla miniatura), lista con filtros, creación perezosa.
- **T004 Wizard completo**: etapa 8 en acordeón (validaciones duras: 00:00, tarjetas 60s/120s con borrador local que no persiste estados inválidos), botón Marcar como PUBLICADO con requisitos+modal fecha+confetti canvas propio, etapa 9 con día N y difusión, etapa 10 con glosario de estrategias por familia y análisis IA, dashboard KPIs+continuar, detalle con estado manual y eliminar con Deshacer.
- **T005 Curso+Plantillas**: API curso/plantillas, 3 vistas curso (placeholder regla de oro #1, notas autosave, % por sección con color de familia), biblioteca con filtros y detalle con variables→preview→copiar/descargar md-txt/duplicar/editar.
- **T006 IA+Métricas**: `prompts.js` con los 9 generadores LITERALES del 04, `extraerJson` balanceado, flujo reintento→degradación elegante, AIInteraction con modeloUsado real y costo 0 en :free, transporte inyectable (tests con stub), métricas completas (unicidad por fecha → DUPLICATE_SNAPSHOT, velocidad calculada con días de calendario, resumen agregado, insights heurísticos) y panel Recharts + SnapshotModal compartido con etapa sprint.
- **T007 Cierre**: export/import JSON v1 (replaceAll/fusionar, IMPORT_VERSION_MISMATCH, test ida-y-vuelta entre dos instancias), UI de backup en Configuración, README de arranque, a11y (skip-link, focus ring, aria-live en autosave/toasts, reduced-motion también en confetti).

Verificación final: **42/42 tests backend · tsc limpio · vite build OK · init.sh "Proyecto listo" · smoke test OK**.

Archivos tocados: `package.json` raíz (workspaces), `app/backend/**` (12 src + 5 suites de test), `app/frontend/**` (~45 archivos), `scripts/smoke.mjs`, `README.md`, `.gitignore`, `TASKS.json` (7/7 completadas).
Problemas y arreglos: falso positivo init.sh (python ausente → fallback a node), bug de redondeo en diasDesdePublicacion (instantes vs días de calendario, corregido con test), un Edit que rompió Onboarding.tsx (reparado), typo `valorPorPorDefecto` en 05 (corregido antes de generar seeds).

### [T001 — COMPLETADA] Guía maestra terminada (resto de archivos)
Qué se hizo: `04_MODULO_IA.md` (9 generadores con prompts literales, cliente OpenRouter, errores/fallbacks, modelos :free verificados jun-2026: openrouter/free defecto, llama-3.3-70b, gpt-oss-120b, qwen3-next, gemma-4; límites 20/min 200/día), `scripts/build_seeds.mjs` (parsea 05 y 07 → genera seeds; verificado: 20 secciones/169 asignaturas/25 plantillas), `05_plantillas_seed.json`, `07_curso_seed.json`, `08_ROADMAP_Y_TESTS.md` (stack fijado, layout workspaces, estrategia de tests, 6 sprints con criterios), `09_PROMPTS_CLAUDE_CODE.md`. Coherencia: 00 (stack fijado, 25 plantillas, fuera ref narrativa), 01 (stack/árbol/env), 05 (título 25 + typo valorPorPorDefecto), `.env`/`.env.example` nuevos, `init.sh` valida JSON con node→python→aviso (fix del falso positivo).
Archivos tocados: creados `app/guia_maestra/04_MODULO_IA.md`, `08_ROADMAP_Y_TESTS.md`, `09_PROMPTS_CLAUDE_CODE.md`, `05_plantillas_seed.json`, `07_curso_seed.json`, `scripts/build_seeds.mjs`; modificados `00`, `01`, `05`, `.env`, `.env.example`, `scripts/init.sh`, `TASKS.json` (T001 completada, T002 en progreso).
Problemas: ninguno.

### [T001 — parcial] Completar Guía Maestra
Qué se hizo: investigación completa del estado del kit (00/01/03/05/06/07 existen y son coherentes; faltan 02, 04, 08, 09 y los 2 seeds JSON). Decisiones tomadas con el usuario vía preguntas:
- Fuente del método: derivar de los archivos existentes (la guía narrativa original no está en el repo).
- Stack FIJADO: React+Vite / Express / SQLite (BD embebida, cero dependencias externas).
- IA: OpenRouter con modelo gratuito (`:free`) como defecto — verificar slug vigente al escribir 04.
- Entrega archivo a archivo, orden: 02 → 04 → seeds → 08 → 09 → pasada de coherencia (00, 01, .env).

Archivos tocados:
- creado: `app/guia_maestra/02_FLUJOS_Y_UX.md` (onboarding de 9 pasos + wizard 10 etapas con checklists auto/manual por etapa, mapeo etapa↔estado, reanudación, bloque IA, flujos secundarios, edge cases, convención data-testid)
- creado: `progress/2026-06-10.md`

Problemas: ninguno en la tarea.

---

## Tareas bloqueadas

Ninguna.

---

## Notas
- `scripts/init.sh` da 1 error FALSO POSITIVO: valida TASKS.json con `python`, que no existe en el PATH del bash de este entorno (Windows). TASKS.json verificado como JSON válido vía PowerShell `ConvertFrom-Json`. Candidato a `/improve`: añadir fallback a node/PowerShell en init.sh.
- `05_PLANTILLAS.md` dice "23 plantillas" pero lista 25 → en la pasada de coherencia se actualizará el título a 25.
- `00_INDICE_MAESTRO.md` referencia `GUIA_MAESTRA_CRECETUBE_APP.md` (narrativa original) que no está en el repo → se quitará la referencia en la pasada de coherencia.
- Decisión de diseño en 02: checklist con items `auto` (derivados del estado del VideoProject, no persistidos) e items `manual` (persistidos vía PATCH /checklist). El wizard nunca bloquea avanzar; única acción con requisitos: "Marcar como publicado".
