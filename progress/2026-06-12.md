# Progreso — 2026-06-12

## Tanda final del día: demo + diseño IA-por-campo + wrap

### T023 ✅ — Modo demo EN PRODUCCIÓN
"Probar la demo" en /acceso → POST /api/auth/demo crea cuenta efímera con seed "Recetas en 15" (2 canales, 6 vídeos en etapas variadas con snapshots progresivos 412→1530→4870, 7 clases del curso, viabilidad hecha), badge DEMO, migración v5 (users.esDemo) + purga a 7 días de las 9 tablas. 1er intento del implementor murió por límite de sesión SIN escribir; el 2º completó todo. Reviewer APROBADO sin arreglos (104/104 backend, 8/8 E2E). Verificado en producción: 201, 6 vídeos, 2 canales.

### T024 (exploración hecha) + T025 (definida) — IA por campo
Visión del usuario: todo campo de texto rellenable con IA, con requisitos por campo (pestañita con lo que falta), reglas Romuald por campo, y viabilidad como primer paso del flujo sin canal. Explorer FABLE (model-override) entregó el diseño completo (38 campos, arquitectura rellenar_campo+campos.js+FieldIA, 4 lotes) → en progress/explorer-log.md y resumido en SESSION.md. Implementación para el próximo hilo.

### Verificación "onboarding solo una vez" (pregunta del usuario)
Test en producción con la cuenta QA: crear perfil → segundo login → perfil persiste (200) → el cuestionario no reaparece. Lo que vio el usuario fue por sus respuestas pre-fix en la sesión anónima compartida.


## Continuación de la sesión larga (arrancó el 2026-06-11)

### Cambios en el harness (instrucción directa del usuario)
- Modelos de agentes (.claude/agents/): implementor sonnet→**fable**; explorer se queda en sonnet; reviewer se queda en opus.
- Agente NUEVO `redactor` (sonnet): redacción de asignaturas del curso desde transcripciones, con las reglas del método integradas (tono Romuald, formato pre-wrap, conflicto = gana el vídeo más nuevo). Las próximas tandas de T012 lo usan directamente.

### T016 ampliada (petición del usuario)
Login con email+contraseña ADEMÁS de Google: misma pantalla, hash scrypt de node:crypto (cero deps nuevas), users.passwordHash nullable, SIN recuperación de contraseña de momento (necesitaría servicio de email — mejora futura). El login email/contraseña NO está bloqueado por las credenciales OAuth.
Exploración T016+T017 completa en progress/explorer-log-t016.md: auth opcional (modo local automático sin GOOGLE_CLIENT_ID), deps autorizadas jose+cookie-parser, fases (Fase 0 sin credenciales: migraciones+userId+canal por defecto). Ofrecido al usuario hacer las credenciales de Google Cloud por él vía extensión Chrome (no conectada aún).

### T018 — Fase de viabilidad del canal ✅ COMPLETADA (explorer → implementor FABLE → reviewer)
- Backend: tabla `viabilidad` (singleton id='main'), routes/viabilidad.js GET/PATCH, generador IA `evaluacion_nicho` en prompts.js (veredicto viable/ajustar/pivotar + puntuación + fortalezas/riesgos/siguientePaso), tests viabilidad.test.mjs 6/6.
- Frontend: /viabilidad (stepper 5 pasos: idea → demanda → competencia/hueco → sub-nicho/PVU → veredicto con AiBlock), autosave 800ms, "Saltar por ahora" persistido en BD; ítem de sidebar y banner de Dashboard SOLO si profile.tieneCanalYa === false (NAV movido dentro de Layout, cambio frágil verificado); strings en es.ts.
- Reviewer: APROBADO sin arreglos — 48/48 backend, 7/7 E2E, tsc, build.
- Verificación visual del orquestador: página renderiza (stepper, consejo Romuald, 3 campos paso 1), avance a paso 2 "Demanda" OK, API /api/viabilidad 200, sidebar correctamente sin el ítem (perfil local con canal), consola limpia.

### Credenciales OAuth de Google creadas por el agente (vía extensión Chrome) ✅
Con la extensión "Claude for Chrome" conectada, el agente condujo el navegador del usuario (logueado en su cuenta) por console.cloud.google.com: proyecto `crecetube` creado, Google Auth Platform configurada (app "CRECETUBE Assistant", público externo, contacto oscarcon314@gmail.com, Política de Datos aceptada CON confirmación explícita del usuario, que pidió avisar de cualquier coste: todo 0€), cliente OAuth "CRECETUBE Web" (web) con orígenes http://localhost:5173 y https://crecetube.vercel.app, y usuario de prueba añadido. CLIENT_ID guardado en T016. App en modo Prueba (límite 100 usuarios de prueba; "Publicar app" cuando se quiera abrir). Trucos técnicos que funcionaron contra la consola de Google (pesada, selects custom cfc-select): foco por JS + dispatchEvent de pointerdown/mousedown/click en el trigger, valores con native setter + evento input, y verificación por DOM en vez de screenshots (el renderer se congelaba). El usuario también entregó su clave OpenRouter (la pega él en Configuración; modelo por defecto openrouter/free ya es gratuito).

### T016+T017 ✅ COMPLETADAS — multi-usuario, login y multi-canal EN PRODUCCIÓN
Ciclo multiagente en 2 lotes (explorer previo → implementor fable lote 1 backend → implementor fable lote 2 frontend → reviewer opus). Incidencias del camino: el primer agente del lote 2 murió al ~40% por límite de sesión (se relanzó uno fresco con orden de auditar y completar, no rehacer: funcionó); el primer reviewer cayó por corte de socket (relanzado); 12 BDs E2E huérfanas de runs abortados rompían Playwright (limpiadas por el reviewer).
- Backend: migraciones v1-v4 (users, channels+adopción, PKs compuestas), middleware modo local, auth scrypt/Google JWKS/cookie JWT, todas las rutas escopadas por userId (auditadas una a una), CRUD canales. 73/73 tests (+25).
- Frontend: /acceso (registro/login + botón Google), sesión en sidebar con Salir, onboarding 10 pasos con pregunta multicanal, Proyectos (rejilla de canales) si gestionMulticanal, curso/viabilidad/tutorial intactos.
- Migración validada sobre COPIA de la BD real (v0→v4, vídeo adoptado por "Canal de Oscar") y aplicada en Turso producción sin pérdida.
- Vercel: SESSION_SECRET (generado) y GOOGLE_CLIENT_ID añadidos a producción vía CLI. Deploy READY.
- Smoke en producción: registro 201 (cookie httpOnly+secure), me=cuenta, password mala 401, sin cookie modo local. Queda un usuario QA de prueba (qa.smoke@crecetube.test) — borrable.
- Nota: el QA visual local quedó aplazado porque un python.exe del usuario (PID 17000, desde las 0:39) ocupa el puerto 8001; cubierto por E2E del reviewer + verificación en producción.

### T019+T020 ✅ COMPLETADAS — recomendador IA de temas y sello "Romu aprueba" EN PRODUCCIÓN
Ciclo: explorer T019 (en paralelo con T016/17) → implementor fable T019 → implementor fable T020 → reviewer opus conjunto (APROBADO sin arreglos a la primera).
- corpus.js: destila s3/s4/s6 del seed (solo clases con contenido; 31.105 chars brutos → 3.994 con guardia dura de 4.000) — mejora solo con cada tanda del curso.
- temas_canal: 5 ideas (título, ángulo, por qué funciona según el método, formato, dificultad), evita títulos ya usados (truncado a 2.000 chars), escopado por userId. Bloque "Ideas para tu próximo vídeo" en Dashboard.
- romu_aprueba: veredicto aprobado/ajustar + puntuación + ≤6 puntos + resumen en tono Romuald; guardias 6.000 chars; RomuAprueba.tsx en 8 etapas del wizard (mapa de campos por etapa; grabación/edición excluidas con criterio).
- Suites: 78/78 backend, 7/7 E2E, tsc, build. QA visual: bloque de ideas en Dashboard y sello en wizard/titulo renderizando con degradación elegante sin clave; consola limpia. Deploy READY.
- Pendiente del usuario: crear su cuenta y pegar su clave OpenRouter para estrenar los generadores en vivo (el agente NO introduce claves: regla de seguridad).
- También: el usuario pidió matar el python.exe del puerto 8001 → eliminado, backend dev restaurado (migraciones locales OK, "Canal de Oscar" como canal por defecto).

### Fix login-first en producción (corrección de diseño de T016, avisada por el usuario)
El usuario entró a crecetube.vercel.app y le salió el onboarding directamente: con auth configurada, los visitantes anónimos compartían el usuario 'local'. Arreglo: RequireSesion en App.tsx — si authConfigurada y modo local → /acceso. El modo local queda solo para uso en el propio PC sin auth. Verificado local (no se activa) y desplegado (commit 0c1fbfa).

### T021+T022 ✅ COMPLETADAS — onboarding adaptativo y cadena del método EN PRODUCCIÓN
Diseño propuesto por el orquestador y aprobado por el usuario con 3 decisiones (AskUserQuestion): recomendación fuerte (no obligatoria) hacia viabilidad, generador de nombres SÍ, bloqueo DURO. Ciclo: explorer conjunto → implementor fable lote A (T021) → implementor fable lote B (T022) → reviewer opus conjunto (APROBADO sin arreglos).
- T021: onboarding bifurcado (9 pasos con canal / 8 sin él, numeración dinámica), "Todavía no"/"Aún no lo sé" con nulabilidad completa (el implementor cazó además TemplateDetail, que no estaba en el inventario), saludo "Hola, creador", banner de viabilidad como recomendación fuerte, e2e nuevo de la rama sin canal con limpieza /api/import.
- T022: requisitos.js fuente única del orden del método (11 generadores con requisitos sobre campos reales, verificados con flujo API real: 422 → completar → 200), AiBlock bloqueado (candado + mensaje en tono Romu + "Ir al paso →"), generador sugerir_nombres_canal (Viabilidad + Settings con "Usar este").
- Suites: 94/94 backend (+16), 8/8 E2E x2, tsc, build. Deploy READY (f5ce0da).
- El reintento con espera larga (~35 min) venció el rate limit: 3/3 transcripciones (Talent Land 2025 8.9k palabras, entrevista 2017 4.7k, ESDR 2026 15.8k).
- Lote G7 con el agente `redactor` nuevo: 7 asignaturas (s4_a4/a5/a8 nichos, s17_a5 productos digitales, s20_a1/a2/a5 mentalidad). Criterio honesto: entrevista 2017 descartada como fuente primaria; ESDR 2026 solo aportaba para 2 clases.
- **Incidencia de calidad**: G7 llegó escrito sin tildes ni eñes (0-8 acentos/clase vs media 45 de la tanda 1). Detectado en el muestreo del orquestador, corregido por agente quirúrgico (37-53 acentos/clase finales, +"Hacete"→"Hazte"), y regla de ortografía blindada en redactor.md y _instrucciones.md. 1ª aparición del patrón "agente escribe español sin tildes" — si reaparece, va a improvements/.
- Fusión a seed v4, 48/48 backend, verificado local y producción (63 con contenido + videoRef). Commit 5038b45.
